import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sk = new Scanner(System.in);
        ///Realizar un programa que lee por teclado el valor del radio de una circunferencia, calcula y muestra
        //por pantalla la longitud y el área de la circunferencia. Cálculo de longitud: 2 * PI * radio, cálculo de
        //área: PI * radio^2. Usar la clase Math.
        /*
        System.out.println("Ingrese el radio de una circunferencia:");
        double radio = sk.nextDouble();

        double longitud = 2 * (Math.PI) * radio;

        double area = (Math.PI) * (Math.pow(radio,2));

        System.out.println("Longitud del radio de la circunferencia:" + longitud);
        System.out.println("Area del radio de la circunferencia:" + area);


        ///Realizar un programa que lea la longitud de los catetos de un triángulo rectángulo y calcule la
        //longitud de la hipotenusa según el teorema de Pitágoras. Usar la clase Math.
        System.out.println("Ingrese la longitud del cateto1:");
        double longCateto1 = sk.nextDouble();
        sk.nextLine();
        System.out.println("Ingrese la longitud del cateto2:");
        double longCateto2 = sk.nextDouble();

        double elevado1 = Math.pow(longCateto1,2);
        double elevado2 = Math.pow(longCateto2,2);

        double sumaElevadosCuadrados = elevado1 + elevado2;

        double raizCuadradaElevadosCuadrados = Math.sqrt(sumaElevadosCuadrados);

        System.out.println("longitud de la hipotenusa según el teorema de Pitágoras:" + raizCuadradaElevadosCuadrados);


        ///Realizar un programa que calcula el volumen de una esfera. El valor de radio se debe pedir por
        //teclado. Cálculo: volumen = 4/3 * PI * radio^3. Usar la clase Math.
        System.out.println("Ingrese el radio de una esfera:");
        double radioEsfera = sk.nextDouble();

        double volumen = (4.0/3.0) * (Math.PI) * (Math.pow(radioEsfera,3));

        System.out.println("Volumen de la esfera: " + volumen);


        Libro libroA = new Libro("El quijote","Miguel Servantes",500,10);
        Libro libroB = new Libro("Cien anios de soledad","Gabriel Garcia Marquez",700,5);

        libroA.verDetalleLibro();
        libroB.verDetalleLibro();

        libroA.vendiendoLibro(3);
        libroA.verDetalleLibro();

        libroB.vendiendoLibro(8);
        libroB.agregandoLibros(5);
        libroB.verDetalleLibro();
         */


    }
}